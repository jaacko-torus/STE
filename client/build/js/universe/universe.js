// let universe = {}; // contains info about every physical and virtual body
// universe.modules    = new Map(); // format `<coords>`
// universe.capsules   = new Map(); // format `<coords>`
// universe.spaceships = new Map(); // format `<ss_name>`
// universe.users      = new Map(); // format `<user_name>`

let universe = {
	modules: new Map(),
	users: new Map()
};

export { universe };